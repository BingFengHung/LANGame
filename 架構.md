# 純前端 GitHub Pages + 閉環區域網路 WebRTC 連線架構設計書 (專業版)

> **專案目標**：打造一款託管於 GitHub Pages、**100% 零伺服器成本**、且在載入後**完全不走外網**的低延遲多人連線遊戲架構（特別適合如 LF2 小朋友齊打交等即時動作格鬥遊戲）。

---

## 1. 核心設計理念與架構優勢

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 唯一外網連線 (僅前 3 秒)                                    │
│    [所有玩家設備] ───── (HTTPS 下載 HTML/JS) ────> [GitHub Pages]   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 閉環信令交換 (Air-Gapped Handshake - 零外網)              │
│    [客端 2, 3, 4] <── (極簡 QR Code 互掃 / 代碼) ──> [房主 Host]   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 100% 純區域網路即時對戰 (星狀拓撲，封包不出家門，延遲 1~3ms)  │
│    [客端 2] <═══════╗                                       │
│    [客端 3] <═══ (WebRTC DataChannel / UDP) ═══> [房主 Host] │
│    [客端 4] <═══════╝       (Wi-Fi 內部轉發)                 │
└─────────────────────────────────────────────────────────────┘
```

### 四大核心優勢
1. **零外部伺服器（$0 Cost）**：無需租用雲端主機、無需資料庫、無需信令伺服器。
2. **極限低延遲（Ultra-low Latency: 1~3ms）**：封包僅在同一個 Wi-Fi 路由器內交換，無外網繞道問題，操作手感與單機同機多打無異。
3. **物理級資安防護（Air-Gapped Privacy）**：
   * 不洩漏真實對外公網 IP，外部黑客無法探測、無法發動 DDoS 攻擊。
   * 封包經過 WebRTC 原生 DTLS 加密，區網內其他未授權設備亦無法窺探。
4. **瀏覽器安全標準合規（No Mixed-Content Block）**：
   * GitHub Pages 強制 HTTPS；一般 WebSocket 在 HTTPS 下嚴禁連線區網（Mixed Content 錯誤）。
   * WebRTC 天生符合瀏覽器安全規範，是目前**唯一能在 HTTPS 靜態網頁上直接打通純區網連線**的合法標準。

---

## 2. 多人連線網路拓撲設計（星狀 Host-Client 架構）

在 3 ~ 4 人連線情境下，若採用傳統 P2P 網狀（Mesh）架構，4 個人每人需與其他 3 人建立連線（共需建立 $4 \times 3 / 2 = 6$ 條連線），光是「互相掃描 QR Code」就會變成災難。

因此，本架構**強制採用「星狀主客架構（Star Topology）」**：

```
        ┌──────────────┐
        │  客端玩家 2   │
        └──────┬───────┘
               │ (RTCDataChannel 1)
               ▼
┌──────────────┐ (RTCDataChannel 2) ┌──────────────┐
│  客端玩家 3  ├──────────────────>│ 房主主機 Host │
└──────────────┘                    │ (權威邏輯判定)│
               ▲                    └──────▲───────┘
               │ (RTCDataChannel 3)        │
        ┌──────┴───────┐                   │
        │  客端玩家 4   │───────────────────┘
        └──────────────┘
```

### 星狀架構運作原則：
1. **房主（Host）兼任遊戲伺服器**：房主設備負責計算物理碰撞、招式判定、扣血計算，並擁有最高權威（Authoritative Server）。
2. **配對流程極簡化**：
   * 只有**房主需要出示 QR Code**。
   * 玩家 2、3、4 依序與房主進行 1 對 1 配對，玩家之間完全不需要互掃。
3. **數據同步機制**：
   * 客端玩家 2、3、4 每秒向房主發送自己的按鍵輸入（Input Payload）。
   * 房主彙整所有人操作後，每秒廣播最新遊戲狀態快照（World Snapshot）給所有客端渲染。

---

## 3. 系統詳細架構階段分解

### 階段一：靜態載入與離線就緒（Initial Load & PWA）
1. 玩家使用手機或電腦瀏覽器打開 GitHub Pages 網址，下載遊戲本體（Canvas 渲染引擎、遊戲素材、邏輯腳本）。
2. **PWA 離線快取（Progressive Web App）**：
   * 配置 `manifest.json` 與 `sw.js`（Service Worker）。
   * 第一次載入成功後，靜態資源全部存入本地 Cache。
   * **即便家裡網路完全斷網（外網中斷、光纖挖斷），只要 Wi-Fi 路由器通電，打開瀏覽器照樣能載入遊戲並進行區網對戰！**

---

### 階段二：零外網信令交換（Air-Gapped Signaling）
WebRTC 連線前必須交換連線描述資訊（SDP 與 ICE 候選地址）。為達成 100% 閉環，強制關閉外網探測，並實施 SDP 瘦身：

#### 1. 關鍵設定：強制禁用外網探測
```javascript
const rtcConfig = {
  iceServers: [] // 👈 核心關鍵：空陣列，徹底阻止任何往外網發送的探測封包
};
const peerConnection = new RTCPeerConnection(rtcConfig);
```

#### 2. SDP「極限瘦身術」（SDP Munging）
原生 WebRTC SDP 往往長達 2,000 ~ 3,000 字元（包含大量音訊、視訊編解碼規格）。過於龐大的內容會生成極密集的 QR Code，導致低階鏡頭對焦不良、掃描失敗。
* **瘦身技巧**：本專案僅使用純數據通道（`RTCDataChannel`），可以在產出 Offer/Answer 時，以正規表達式剔除冗餘屬性：
```javascript
// SDP 瘦身範例：只保留 DataChannel 必需項目
function slimSDP(sdp) {
  return sdp
    .split('\r\n')
    .filter(line => !line.startsWith('a=rtcp') && !line.startsWith('a=extmap'))
    .join('\r\n');
}
```
* **二次壓縮**：搭配 `pako` (zlib) 壓縮後轉成短 Base64，字串長度可壓至 **200 字元以內**，生成顆粒超大、超清晰的 QR Code，任何手機與筆電鏡頭 0.1 秒秒掃！

#### 3. 信令交換模式：

| 模式 | 適用設備 | 操作方式 | 優點 |
| :--- | :--- | :--- | :--- |
| **模式 A：QR Code 互掃** | 筆電（有攝影機）<br>手機 / 平板 | 1. 房主螢幕顯示 Offer QR Code<br>2. 玩家開鏡頭掃描，螢幕回顯 Answer QR Code<br>3. 房主鏡頭掃描確認 | • 全程 0 鍵盤輸入<br>• 光學傳輸，徹底無網路依賴<br>• 最具科技感 |
| **模式 B：連線代碼貼上** | 一般桌上型電腦<br>（無鏡頭設備） | 1. 房主點擊「複製房間碼」（Base64 壓縮 SDP）<br>2. 透過區網共享文字/即時通訊發送給朋友<br>3. 朋友貼上後回傳 Answer 碼 | • 相容所有設備與瀏覽器<br>• 開發實作最簡單快速 |

---

### 階段三：純區網即時資料傳輸（LAN Gameplay Transport）
連線通道一旦打通，瀏覽器將建立 `RTCDataChannel`：

```javascript
// 針對即時格鬥動作遊戲（如 LF2）的最佳化參數
const dataChannel = peerConnection.createDataChannel("gameControls", {
  ordered: false,          // 動作遊戲允許微量無序（避免因掉包造成遊戲暫停）
  maxRetransmits: 0        // 立即發送，不重複重傳舊按鍵（以最新影格狀態為準）
});
```

* **通訊協定**：基於 UDP 的 SCTP 封包。
* **延遲表現**：Wi-Fi 內部直轉僅 **1 ~ 3 ms**。
* **頻寬負載**：每秒發送 60 次輕量按鍵封包（每秒 < 5 KB），對家用 Wi-Fi 完全零負擔。

---

## 4. 現代瀏覽器環境與底層相容性對策

### 1. 現代瀏覽器「mDNS 本地 IP 遮蔽機制」因應
現代瀏覽器（Chrome / Edge / Safari）為保護用戶隱私，在 WebRTC 預設啟用 mDNS 機制：
* 瀏覽器不會直接暴露 `192.168.1.100`，而是輸出例如 `4b7c89f2-....local` 的虛擬主機名。
* **因應方案**：
  * 同一 Wi-Fi 網段下，系統的多播（Multicast DNS / 224.0.0.251）能自然解析 `.local`。
  * 若在部分不支援 mDNS 多播的舊型路由器上連線失敗，可在 UI 提示引導用戶在 Chrome 網址列開啟 `chrome://flags/#enable-webrtc-hide-local-ips-with-mdns` 並設為 `Disabled` 即可直連真實 IP。

### 2. 行動設備防休眠與斷線保護
若有玩家使用手機或筆電瀏覽器同樂，容易因切換 App 或螢幕變暗導致 WebRTC 被系統背景凍結：
* **啟用 Screen Wake Lock API**：
  ```javascript
  // 遊戲進行時鎖定螢幕常亮，防止手機休眠斷線
  if ('wakeLock' in navigator) {
    navigator.wakeLock.request('screen').catch(console.error);
  }
  ```
* **心跳偵測（Heartbeat Ping-Pong）**：
  * 房主每 2 秒發送一次 Ping 封包，客端即時回應 Pong。
  * 若超過 4 秒無回應，UI 顯示「連線暫時中斷」，並保留玩家角色狀態 30 秒，允許客端透過「快速重連」重新接回對戰。

---

## 5. 網路環境前置檢查與踩坑指南（Troubleshooting）

在區網連線開發與部署時，最常遇到的實體網路地雷為**路由器的 AP 隔離功能**：

| 常見地雷 | 原因分析 | 解決方法 |
| :--- | :--- | :--- |
| **同一個 Wi-Fi 卻無法打通** | 家用路由器開啟了 **「AP 隔離 (Client/AP Isolation)」** 或連到了**「訪客網路 (Guest Wi-Fi)」**，阻擋內網設備互通。 | 登入分享器後台，關閉 AP Isolation，並確保所有玩家連線至主要 Wi-Fi 名稱（SSID）。 |
| **2.4G 與 5G 頻段互連障礙** | 一人連 2.4GHz Wi-Fi、一人連 5GHz Wi-Fi，部分舊分享器跨頻段轉發被限制。 | 建議所有玩家統一連線至 5GHz 頻段（延遲更低且干擾少）。 |
| **電腦防火牆阻擋 UDP** | Windows Defender 防火牆將公用網路的 UDP 入站封包攔截。 | 在 Windows 網路設定中將當前 Wi-Fi 設為「私人網路（Private Network）」。 |

---

## 6. 完整資料流序向圖（Sequence Diagram）

```
[客端玩家 2, 3, 4]             [家用 Wi-Fi 路由器]            [房主主機 Host]             [GitHub Pages]
        │                              │                           │                          │
        ├───────────────────────── (1) 下載遊戲靜態代碼 ─────────────────────────────────────>│
        │                              │                           ├─────── (1) 下載代碼 ─────>│
        │ (載入完成，斷開外網依賴)        │                           │ (載入完成，啟動 PWA 快取) │
        │                              │                           │                          │
        │                              │                           ├── [生成 Slim SDP Offer]   │
        │                              │                           │   (瘦身後 <200 字元)      │
        │<══════════════════════ (2) 螢幕顯示清晰大顆粒 QR Code ═══════════┤                          │
        │ (相機掃描 0.1 秒讀取)           │                           │                          │
        ├── [生成 Slim SDP Answer]      │                           │                          │
        │═══════════════════════ (3) 客端螢幕回顯 Answer QR Code ═════════>│                          │
        │                              │                           │ (房主掃描確認)            │
        │<====================== (4) 建立星狀 WebRTC 區網通道 =============>│                          │
        │                              │                           │                          │
        ├── [發送操作按鍵 (Input)] ─────┼──────────────────────────>│ (每秒 60 次)             │
        │                              │                           ├── [權威計算物理與碰撞]    │
        │<── [接收世界狀態 (Snapshot)] ─┼───────────────────────────┤ (每秒 60 次廣播)         │
        │    (延遲 1~3ms，不經由外網)    │                           │                          │
```

---

## 7. 推薦技術棧選型（Tech Stack）

1. **靜態前端託管與快取**：
   * **GitHub Pages**：免伺服器，自動 HTTPS。
   * **Workbox / Service Worker**：支援離線啟動（PWA）。
2. **WebRTC 壓縮與信令輔助套件**：
   * **pako**：將 SDP 進行 zlib 壓縮，縮短字串長度。
   * **qrcode.js**：前端高效繪製大顆粒、易掃描的 QR Code。
   * **html5-qrcode**：跨平台（相容 iOS Safari 與 Android Chrome）的前端相機掃描庫。
3. **遊戲核心引擎**：
   * **Canvas 2D 原生 API / PixiJS**：極致輕量，還原經典 LF2 像素打擊手感。
4. **網路通訊與同步架構**：
   * **RTCDataChannel (UDP)**：`ordered: false`, `maxRetransmits: 0`。
   * **Host-Client Authoritative Sync**：星狀主客同步，杜絕多人生產者不同步問題。

---

## 8. 專案落地方案與實作里程碑（Milestones）

- [ ] **Phase 1: 基礎 POC 驗證**
  * 建立單一 HTML 檔案，配置 `iceServers: []`。
  * 透過手動複製代碼，驗證同 Wi-Fi 兩台電腦能否傳送 "Hello World"。
- [ ] **Phase 2: SDP 瘦身與 QR Code 光學握手**
  * 實作 `slimSDP()` 剔除多餘影音屬性並透過 `pako` 壓縮。
  * 整合 `qrcode.js` 與 `html5-qrcode`，實現 5 秒內免打字光學配對。
- [ ] **Phase 3: 3~4 人星狀拓撲（Host-Client）通道打通**
  * 房主介面實作「房間大廳」，能同時維護 3 條客端 DataChannel。
  * 實作基本的心跳 Ping-Pong 與 Wake Lock 防休眠。
- [ ] **Phase 4: LF2 核心遊戲與按鍵同步**
  * 實作 LF2 角色動作狀態機（走動、跳躍、防禦、氣功波）。
  * 房主進行碰撞權威判定，廣播 60 FPS 狀態快照。
- [ ] **Phase 5: PWA 離線快取發布**
  * 編寫 `manifest.json` 與 `sw.js`，推送至 GitHub Pages，實現全斷網環境下的終極離線對戰。
